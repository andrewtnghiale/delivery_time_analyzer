# delivery_time_analyzer
End-to-end shipment ETL and analytics project. Generates mock logistics data, performs extensive cleaning (IDs, dates, regions, outliers), loads the dataset into SQLite, and runs 25+ SQL analysis queries to evaluate delivery performance, delays, and regional trends. Includes optional multiprocessing for large datasets and visual dashboards.
